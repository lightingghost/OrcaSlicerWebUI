# API Authentication Secret
# Must match the API_SECRET configured in the backend
# 
# NOTE: When using run-local.sh, this is automatically passed from the script.
# This .env file is only needed if you're running the frontend standalone:
# npm run dev
#
VITE_API_SECRET=test-secret-key
