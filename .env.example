# API Authentication Secret
# This secret is used by both the backend and frontend for authentication.
# Generate a secure random value for production:
#   openssl rand -hex 32
#
# For local development with run-local.sh, this is set in the script itself.
# For Docker deployment, create a .env file with this variable.
API_SECRET=changeme
